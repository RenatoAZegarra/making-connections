function changeName(element) {
  element.innerText = "Chad Johnson"
}
function deleteName(element){
  element.remove();
}
function addConnection(element,element2){
  element.innerText = element.innerText-1
  element2.innerText++;
}